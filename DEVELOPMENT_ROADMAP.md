# KODAnnai 開発ロードマップ

> **作成日**: 2026年4月12日  
> **最終更新**: 2026年4月30日  
> **現在のバージョン**: コア機能 + 建物MAP連携実装済み  
> **目標**: KODAIRA祭当日に来場者が快適に使える完成度へ

---

## 目次

1. [現在地（完成済み機能）](#現在地完成済み機能)
2. [フェーズ1: 品質向上・バグ修正（短期）](#フェーズ1-品質向上バグ修正短期)
3. [フェーズ2: マップから出発地を選ぶ体験の改善](#フェーズ2-マップから出発地を選ぶ体験の改善)
4. [フェーズ3: UX 改善・演出（中期）](#フェーズ3-ux-改善演出中期)
5. [フェーズ4: 運用・パフォーマンス（本番前）](#フェーズ4-運用パフォーマンス本番前)
6. [フェーズ5: 発展的機能（余裕があれば）](#フェーズ5-発展的機能余裕があれば)
7. [技術メモ](#技術メモ)

---

## 現在地（完成済み機能）

| 機能                              | 状態 | 備考                                                      |
| --------------------------------- | ---- | --------------------------------------------------------- |
| Dijkstra 経路探索                 | ✅   | 雨天/バリアフリーモード対応                               |
| 写真ナビゲーション                | ✅   | NODE_TRIPLETS 472件 + 画像512枚（PDF→PNG変換済）          |
| 施設データ                        | ✅   | LOCATIONS配列 全施設登録済                                |
| ダークモード                      | ✅   | SVGマップ含む全UI対応                                     |
| 屋外マップから出発地選択          | ✅   | SVGマーカータップ方式（12マーカー）                       |
| テキスト検索で出発地/目的地選択   | ✅   | カテゴリフィルタ付き                                      |
| URL共有                           | ✅   | クエリパラメータでルート保持                              |
| 2ノードパス対応                   | ✅   | 隣接ノード間ルーティング修正済                            |
| 企画本館MAP連携                   | ✅   | 屋外MAPの本館エリアタップ→建物MAP表示、5マーカー          |
| 企画別館MAP連携                   | ✅   | 屋外MAPの別館エリアタップ→建物MAP表示、3マーカー          |
| ちびっこ館MAP連携                 | ✅   | 屋外MAPのちびっこ館建物タップ→建物MAP表示                 |
| マーカー定義の一元管理            | ✅   | locations-server.tsx に全マーカー集約                     |
| ゴミステーション最寄り分析        | ✅   | 5ゴミステーションの近接エリアマッピング完了               |
| PWA化（アイコン・manifest）       | ✅   | app/manifest.ts・icon.png(512px)・apple-icon.png(180px)整備 |
| OGP・SNSシェア設定                | ✅   | og-image.png・layout.tsx metadata完備・description修正済み |
| コード整理                        | ✅   | page.tsx不要import削除・未使用画像削除・manifest一元管理  |

---

## フェーズ1: 品質向上・バグ修正（短期）

> **目安**: 1〜3日  
> **優先度**: 🔴 高

### 1-1. ちびっこ館MAP 出発地マーカー追加 ✅

- [x] ちびっこ館内のノード座標を特定
- [x] `CHIBIKKO_START_POINT_MARKERS` 配列を locations-server.tsx に追加
- [x] outdoor-navigation-map.tsx にちびっこ館マーカーのオーバーレイを実装

### 1-2. 企画別館MAP 階数ラベル ✅

- [x] 1F/2F/3F のラベルを各階の図の左上（建物外側）に追加

### 1-3. ナビゲーション画像の最終チェック

- [ ] 全画像が正しいか目視確認
- [ ] コピーで暫定対応した画像の差し替え（`63_263_269.png`, `63_263_284.png`, `63_263_300.png`）
- **担当**: 手動（現地撮影が必要なものは本人対応）

### 1-4. エッジケースのテスト

- [ ] 同一地点（出発=目的地）を指定した場合の挙動
- [ ] 全カテゴリの施設から実際にルート検索を実行して破綻がないか
- [ ] 雨天モード + バリアフリーモード同時有効でルートが存在するか
- **担当**: 手動テスト / AI でテストスクリプト作成可

### 1-5. レスポンシブ・アクセシビリティ

- [ ] スマートフォン（iPhone SE 〜 iPhone 15 Pro Max）での表示確認
- [ ] 各建物MAPの表示サイズ・文字サイズの実機確認
- [ ] タブUIのタッチターゲットサイズ確認（最低44px）
- **担当**: 手動（実機確認）

---

## フェーズ2: マップから出発地を選ぶ体験の改善

> **目安**: 3〜7日  
> **優先度**: 🔴 高（来場者が「自分がどこにいるか」を地図で見つけて出発地に設定できるようにする）

### 現状の仕組み

- `outdoor-navigation-map.tsx` が SVGマップ（ライト/ダーク切替）を表示
- `NODE_MAP_MARKERS` の各ノード座標に円形マーカーを描画
- マーカータップ → 確認ダイアログ → `onSelectNode` コールバックで出発地設定
- `home-page-client.tsx` の「地図から検索」タブから全画面マップを表示
- SVG viewBox: `0 0 295.32 413.16`

### 課題

来場者は地図を見て「自分が今ここにいる」と判断してタップする。しかし現状では:

1. **マーカーが小さい/密集**していてどれをタップすべきかわかりにくい
2. **ズーム操作**が不十分で細かいエリアを選びにくい
3. **建物名・エリア名**がマーカーと対応していないため見つけにくい
4. マップを開くまでに「地図から検索」タブ → 「タップして地図ページを開く」と**2ステップ必要**

### 2-1. マーカーの視認性・タップ性向上

- [ ] マーカーサイズを端末幅に応じて拡大（スマホでは最低タップ領域44px確保）
- [ ] マーカーにラベル（ノード付近の場所名）をツールチップ or 常時表示
- [ ] 選択済みマーカーを目立つ色（現在のブルーリング）+ パルスアニメーション
- [ ] ホバー/タッチ中のマーカーを拡大表示
- **実装場所**: `components/outdoor-navigation-map.tsx`
- **担当**: AI対応可

### 2-2. ピンチズーム・パンの改善

- [ ] マップの拡大・縮小をスムーズにする（CSS transform + touch events / ライブラリ）
- [ ] ズーム時にマーカーのタップ判定が正しく動くようにする
- [ ] ダブルタップでズームイン、ピンチアウトでズームアウト
- **アプローチ候補**:
  - `CSS transform: scale()` + `touch-action: none` で自前実装
  - `react-zoom-pan-pinch` ライブラリの導入（軽量で SVG と相性良い）
- **担当**: AI対応可

### 2-3. エリア分けハイライト

- [ ] マップ上のエリア（正門通り、クラス模擬店エリア、ファミリーエリア等）をタップ可能な領域として定義
- [ ] エリアをタップ → そのエリア内のマーカー一覧を表示 → 選択
- [ ] または、エリアタップで自動的にそのエリアの代表ノードを出発地に設定
- **データ**: `locations-server.tsx` の出発地点プリセット（id: "9001"〜）と連携可能
- **担当**: AI + 手動（エリア境界の座標定義が必要）

### 2-4. マップ直接表示の導線改善

- [ ] 「地図から検索」タブをタップしたら**即座にマップを表示**（現在は「タップして開く」が1枚挟まる）
- [ ] 全画面マップから戻る際に選択状態を保持
- [ ] マップ選択後、自動的にタブ表示に戻り出発地が設定済みであることを明示
- **実装場所**: `app/home-page-client.tsx` のタブ切替ロジック
- **担当**: AI対応可

### 2-5. マップ上の「今ここ」目印サポート

- [ ] ユーザーが自分で「自分はここにいる」とロングタップして任意の位置にピンを立てる機能
- [ ] ピンの位置から最寄りの NODE_MAP_MARKERS ノードを自動選択
- [ ] 最寄りノードまでの距離が遠すぎる場合は警告を表示
- **技術**: SVG上のタッチ座標 → NODE_MAP_MARKERS との最小距離計算（ユークリッド距離）
- **担当**: AI対応可

```typescript
// イメージ: SVG上のタップ座標から最寄りノードを探す
function findNearestMarker(tapX: number, tapY: number): NodeMapMarker | null {
  return NODE_MAP_MARKERS.reduce(
    (nearest, marker) => {
      const d = Math.sqrt((tapX - marker.x) ** 2 + (tapY - marker.y) ** 2);
      return d < nearest.dist ? { marker, dist: d } : nearest;
    },
    { marker: null as any, dist: Infinity },
  ).marker;
}
```

### 2-6. 実装ステップまとめ

| 順番 | 作業                           | 効果                       | 担当      |
| ---- | ------------------------------ | -------------------------- | --------- |
| ①    | マーカーサイズ拡大・ラベル追加 | タップ精度向上             | AI        |
| ②    | タブ→マップ直接表示の導線改善  | 操作ステップ削減           | AI        |
| ③    | ピンチズーム・パン実装         | 密集エリアの操作性         | AI        |
| ④    | ロングタップで任意ピン         | 「自分はここ」の直感的操作 | AI        |
| ⑤    | エリア分けハイライト           | 広い範囲の素早い選択       | AI + 手動 |
| ⑥    | 実機テスト・微調整             | 最終品質確認               | 手動      |

---

## フェーズ3: UX 改善・演出（中期）

> **目安**: 1〜2週間  
> **優先度**: 🟡 中

### 3-1. ルートステップの進捗バー表示 ✅

ズームモーダルのフッター部（前へ/次へボタンの上）に進捗バーを実装済み。

- グレー背景 + 青の達成済みバーでステップ進行を表示
- triplet 1ステップ = 1単位、`(currentZoomIndex + 1) / (routeSteps.length - 1)` で算出
- 「次へ」/「前へ」ボタンで `transition-all duration-500 ease-in-out` の滑らかなアニメーション
- ピクトグラム人形は試行後削除（シンプルなバーのみ）
- **実装場所**: `components/navigation-view.tsx` ズームモーダルフッター

**補足**: SVGマップ上のルート可視化（ノード座標を `<polyline>` で繋ぐ方式）は、全ノードにSVG座標を設定する作業が非現実的なため見送り。

### 3-2. お気に入り・最近の検索

- [ ] よく使うルートをブラウザの localStorage に保存
- [ ] 「最近検索した施設」をトップに表示
- **担当**: AI対応可

### 3-3. 混雑情報（もし可能なら）

- [ ] 特定の施設に「混雑」「空いている」等のステータスを手動設定
- [ ] locations-server.tsx に `congestion?: "low" | "medium" | "high"` を追加
- [ ] 管理者用の簡易UIか、直接データ更新で対応
- **担当**: 設計はAI / 運用データ投入は手動

---

## フェーズ4: 運用・パフォーマンス（本番前）

> **目安**: 本番1週間前  
> **優先度**: 🔴 高

### 4-1. Vercel デプロイ設定

- [ ] 本番ドメインの確認
- [x] OGP画像（SNSシェア用）の設定 ✅
- [ ] `@vercel/analytics` / `@vercel/speed-insights` が有効か確認
- **担当**: 手動（Vercel管理画面）

### 4-2. パフォーマンス最適化

- [ ] SVGファイルの最適化（SVGO等で不要メタ削除、軽量化）
- [ ] 画像の最適化（472枚のPNGを WebP に変換検討）
- [ ] Lighthouse スコア確認（特にモバイル）
- **担当**: AI + 手動

### 4-3. エラーハンドリング

- [ ] `app/api/logerror/route.ts` でクライアントエラーをログ
- [ ] 経路が見つからない場合のユーザーフレンドリーなメッセージ
- [ ] ネットワークオフラインへの対応（Service Worker / PWA化検討）
- **担当**: AI対応可

### 4-4. 負荷テスト

- [ ] 当日の想定アクセス数を見積もり
- [ ] Vercel の無料プランの制限確認（帯域、関数実行数）
- **担当**: 手動判断

---

## フェーズ5: 発展的機能（余裕があれば）

> **優先度**: 🟢 低（あれば嬉しい）

### 5-1. 屋内ナビゲーション対応

- 現在は屋外マップのみ。屋内フロアマップを追加する場合:
  - フロアごとのSVG作成
  - 屋内ノード・エッジの追加
  - フロア切替UIの実装

### 5-2. QRコード連携

- 各施設にQRコードを配置 → スキャンで自動的にその場所を出発地に設定
- URL: `https://kodannai.vercel.app/?from=XXX`

### 5-3. 多言語対応

- 留学生・外国人来場者向けに英語対応
- LOCATIONS の `name` / `description` の i18n

### 5-4. イベントタイムテーブル連携

- 各施設のイベント開始・終了時刻を表示
- 「今やっているイベント」フィルタ

### 5-5. PWA化 ✅

- [x] ホーム画面に追加 → アプリライクな体験（manifest.ts・アイコン整備済み）
- [ ] オフラインキャッシュ（SVG + 画像を事前ダウンロード）← 余力があれば

---

## 技術メモ

### 開発環境

```bash
# 起動
pnpm dev

# ビルド
pnpm build

# 型チェック
npx tsc --noEmit
```

### 主要な依存関係

- **Next.js 14** (App Router)
- **React 18**
- **Tailwind CSS v4** + tw-animate-css
- **next-themes** (ダークモード)
- **Radix UI** (各UIプリミティブ)
- **Lucide React** (アイコン)
- **Vercel** (ホスティング)

### デバッグのコツ

- route-finder.ts が巨大（11,300行）なのでエディタが重い場合は該当セクションだけ開く
- NODE_TRIPLETS の不整合は `scripts/check_locations.js` で検出可能
- SVG の色変更は `.st**` クラスの `fill` / `stroke` を編集（ファイル冒頭の `<style>` ブロック内）

### マップ操作の実装参考（フェーズ2用）

- `outdoor-navigation-map.tsx` の SVG viewBox は `0 0 295.32 413.16`
- `NODE_MAP_MARKERS` は route-finder.ts にエクスポート済み（SVGピクセル座標）
- ピンチズームは `react-zoom-pan-pinch` (npm) か CSS transform ベースで実装可
- SVG上のタッチ座標取得: `SVGElement.getScreenCTM()` の逆行列で変換

---

_このロードマップは進捗に応じて更新してください。_
